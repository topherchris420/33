# C6 — Aerodynamic hinge-moment spring sizing

**Paper claim:** k ≥ 185 N·m/rad with ≥ 20% margin (k_supplied = 220 N·m/rad)

**Recomputed result:**
The deterministic computation yields a required minimum spring rate (k_min) that is successfully verified against the k_supplied. The design margin is verified to be >20% at the specified baseline parameters (v_egress = 70 m/s, Cm_alpha_max = 0.8).

**Artifact paths:**
- `docs/EVIDENCE/C6_spring_sweep.json`
- `docs/EVIDENCE/C6_spring_sweep_plot.png`

*Deterministic computation — no physical measurement*
